services:
  oban:
    image: ghcr.io/derekparks/oban:latest
    build: .
    ports:
      - "${OBAN_PORT:-8080}:8000"
    volumes:
      - ${OBAN_KANBAN_DIR:-./kanbans}:/data/kanbans
    env_file:
      - .env
    environment:
      - OBAN_KANBAN_DIR=/data/kanbans
