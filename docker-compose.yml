services:
  oban:
    image: ghcr.io/derekparks/oban:latest
    build: .
    ports:
      - "${OBAN_PORT:-8080}:8000"
    volumes:
      - ${OBAN_VAULT_DIR:-.}:/data/vault
    env_file:
      - .env
    environment:
      - OBAN_VAULT_DIR=/data/vault
      - OBAN_KANBAN_DIR=/data/vault/Kanbans
